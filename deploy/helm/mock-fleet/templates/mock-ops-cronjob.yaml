{{- if .Values.mockOps.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "mock-fleet.mockOpsFullname" . }}
  namespace: {{ include "mock-fleet.namespace" . }}
  labels:
    {{- include "mock-fleet.labels" . | nindent 4 }}
    app.kubernetes.io/component: mock-ops
spec:
  schedule: {{ .Values.mockOps.schedule | quote }}
  timeZone: {{ .Values.mockOps.timeZone | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            {{- include "mock-fleet.mockOpsSelectorLabels" . | nindent 12 }}
        spec:
          serviceAccountName: {{ required "mockOps.serviceAccount.name is required when serviceAccount.create=false" (include "mock-fleet.mockOpsServiceAccountName" .) }}
          automountServiceAccountToken: true
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: mock-ops
              image: {{ printf "%s:%s" .Values.mockOps.image.repository (.Values.mockOps.image.tag | default .Chart.AppVersion) }}
              imagePullPolicy: {{ .Values.mockOps.image.pullPolicy }}
              securityContext:
                runAsNonRoot: true
                runAsUser: 1001
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop:
                    - ALL
                seccompProfile:
                  type: RuntimeDefault
              env:
                - name: MOCK_FLEET_NAMESPACE
                  valueFrom:
                    fieldRef:
                      fieldPath: metadata.namespace
                - name: MOCK_FLEET_WIREMOCK_VERSION_CATALOG_CONFIG_MAP_NAME
                  value: {{ include "mock-fleet.wiremockVersionCatalogConfigMapName" . | quote }}
                - name: MOCK_FLEET_WIREMOCK_BASELINE_CONFIG_MAP_NAME
                  value: {{ include "mock-fleet.wiremockConfigMapName" . | quote }}
                - name: MOCK_FLEET_WIREMOCK_USER_CONFIG_MAP_NAME
                  value: {{ include "mock-fleet.wiremockUserConfigMapName" . | quote }}
                - name: MOCK_FLEET_WIREMOCK_CONFIG_KEY
                  value: wiremock-options.yaml
                - name: MOCK_FLEET_WIREMOCK_REGISTRY_URL
                  value: {{ .Values.mockOps.registry.url | quote }}
                - name: MOCK_FLEET_WIREMOCK_REPOSITORY
                  value: {{ .Values.mockOps.registry.repository | quote }}
                - name: MOCK_FLEET_WIREMOCK_IMAGE_REPOSITORY
                  value: {{ .Values.mockOps.registry.imageRepository | quote }}
                - name: MOCK_FLEET_WIREMOCK_DEFAULT_VERSION_CONSTRAINT
                  value: {{ .Values.mockOps.defaultVersionConstraint | quote }}
                - name: MOCK_FLEET_WIREMOCK_MINOR_LINES
                  value: {{ .Values.mockOps.minorLines | quote }}
                {{- with .Values.mockOps.registry.credentialsSecretName }}
                - name: MOCK_FLEET_WIREMOCK_REGISTRY_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: {{ . | quote }}
                      key: username
                - name: MOCK_FLEET_WIREMOCK_REGISTRY_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ . | quote }}
                      key: password
                {{- end }}
              resources:
                {{- toYaml .Values.mockOps.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: tmp
              emptyDir: {}
{{- end }}
