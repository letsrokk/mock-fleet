apiVersion: v1
kind: ConfigMap
immutable: true
metadata:
  name: {{ include "mock-fleet.wiremockConfigMapName" . }}
  namespace: {{ include "mock-fleet.namespace" . }}
  labels:
    {{- include "mock-fleet.labels" . | nindent 4 }}
data:
  wiremock-options.yaml: |
    wiremock:
      default:
{{- toYaml .Values.wiremock.config.default | nindent 8 }}
      mocks:
{{- if .Values.wiremock.config.mocks }}
{{- toYaml .Values.wiremock.config.mocks | nindent 8 }}
{{- else }}
        []
{{- end }}
