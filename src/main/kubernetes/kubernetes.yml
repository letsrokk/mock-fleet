---
apiVersion: v1
kind: ConfigMap
metadata:
  name: hazelcast-client-configmap
data:
  hazelcast-client.yaml: |
    hazelcast-client:
      cluster-name: dev
      network:
        cluster-members:
          - {{ .Release.Name }}-hazelcast:5701